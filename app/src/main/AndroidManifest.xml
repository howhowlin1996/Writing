<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.writing">
    <!-- 創建與刪除檔許可權 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <!-- 寫入資料許可權 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--add any activity needs to change-->
        <activity android:name=".coosetype.InOutPage"/>
        <activity android:name=".coosetype.UpDownPage"/>
        <activity android:name=".coosetype.SinglePage"/>
        <activity android:name=".panel.WritingPanel"/>
        <activity android:name=".puzzle.Puzzle"/>
        <activity android:name=".badge.Badge"/>
        <activity android:name=".coosetype.ChooseTypePage"/>
        <activity android:name="main.Setting"/>
        <activity android:name=".badgefactory.BadgeFactory"/>
        <activity android:name=".memo.MemoSelect"/>
        <activity android:name=".panel.CopyWriting"/>
        <activity android:name=".memo.MemoLookUp"/>
        <activity android:name=".memo.MemoEditPic"/>
        <activity android:name="main.FirstScene">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>


</manifest>